node_modules/
ai-doctor/node_modules/
# CI artifacts at repo root only (demo_intelligence_workspace/ keeps its own status+logs)
/reports/
/status/
collected-artifacts/
/build-summary/
demo_intelligence_workspace/build-summary/
.maestro/
temp-runners/
*.flag
*.failed
*.log
!demo_intelligence_workspace/**/*.log
.env
ai-doctor/.env
__pycache__/
*.pyc
*.zip

# Optional nested copy when IDE workspace lives in a subfolder of the same repo
# Pipeline for CI is ONLY tracked at repository root: ./Jenkinsfile
# (Edits to kodak-Smile-with-OpenAI/ are not committed — that folder is ignored here.)
/kodak-Smile-with-OpenAI/
