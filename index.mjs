/**
 * Placeholder so `npm start` works. Maestro + Python are primary; see README.md.
 */
console.log("Kodak Maestro suite — see README.md for setup and test commands.");
console.log("Examples: npm run doctor:junit  |  python -m pytest intelligent_platform/tests");
process.exit(0);
